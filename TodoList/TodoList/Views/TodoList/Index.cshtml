@model IEnumerable<TodoList.Entities.WorkTask>

@{
    ViewData["Title"] = "All Tasks";
}

<h1>All Todo List</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>

<form asp-action="FindWorkTasksByDate" method="get">
    <label for="date">Wybierz datę:</label>
    <input type="date" id="date" name="date" />
    <button type="submit">Szukaj</button>
</form>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ExpectedEndDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IsCompleted)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CreatedAt)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Title)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Description)
                </td>
                <td>
                    @item.ExpectedEndDate.ToShortDateString() <!-- Użycie metody bezpośrednio -->
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.IsCompleted)
                </td>
                <td>
                    @item.CreatedAt.ToShortDateString() <!-- Użycie metody bezpośrednio -->
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                    @Html.ActionLink("Details", "WorkTaskDetails", new { id = item.Id }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.Id })
                </td>
            </tr>
        }
    </tbody>
</table>
